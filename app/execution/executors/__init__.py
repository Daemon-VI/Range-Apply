"""Executor implementations. Real browser executors (local Playwright, the
extension) plug in here in later phases; only the contract, a deterministic
mock and the human-handoff executor exist now."""
